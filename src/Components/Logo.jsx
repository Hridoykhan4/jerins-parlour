import logo from '../assets/images/logo.png'
const Logo = () => {
    return (
        <div>
            <img className='w-[7rem]' src={logo} alt="" />            
        </div>
    );
};

export default Logo;